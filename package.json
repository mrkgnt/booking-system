{
  "name": "booking-system-db-tooling",
  "private": true,
  "type": "module",
  "description": "Seed data generation and DB tooling for the per-tenant Supabase schema. Will likely move under apps/ once the Hono API and Astro site are scaffolded.",
  "scripts": {
    "seed:generate": "tsx db/seed/generate-seed.ts"
  },
  "devDependencies": {
    "@faker-js/faker": "^9.9.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
